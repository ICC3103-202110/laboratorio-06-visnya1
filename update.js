/*function getTip(billAmount, percentage)
{
    const tip = billAmount * percentage /100
    return tip

}

function getTotal(billAmount,tip)
{
    const total = parseInt(billAmount) + parseInt(tip) 
    return total 

}

function update(billAmount, percentage, model)
{
    const newTip = getTip(billAmount,percentage)
    const newTotal = getTotal(billAmount,newTip)
    return{
        ...model,
        billAmount: billAmount,
        percentage: percentage,
        tip: newTip,
        total: newTotal
    }
}

*/
/* module.exports = 
{
    update
}
*/
